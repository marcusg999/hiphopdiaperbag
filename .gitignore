node_modules/
.DS_Store
*.log
shots/
tools/netlog*.json

# Reference material and working files — inputs to the build, not part of the
# deployed site. refs/*.md are the exception: they carry the sourced product
# facts the copy is written from.
refs/*.png
refs/*.json
assets/src/
assets/video/
